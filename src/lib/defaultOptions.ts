const options: {
  cloudinary: {
    cloudName: string;
    uploadPreset: string;
    folder: string;
    multiple: boolean;
  };
} = {
  cloudinary: {
    cloudName: 'b00st',
    uploadPreset: 'uscb5ifq',
    folder: 'B00STButton',
    multiple: true,
  },
};

export default options;
